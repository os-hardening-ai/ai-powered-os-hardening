# --- Core API ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# --- HTTP / Utils ---
requests==2.32.3
httpx==0.27.2
orjson==3.10.12
tenacity==8.5.0

# --- Data / Parsing ---
numpy==1.26.4
pandas==2.2.3
beautifulsoup4==4.12.3

# --- LLM / RAG ---
groq
openai==1.55.3
tiktoken==0.8.0
cohere==5.20.1
langchain==0.3.9
langchain-community==0.3.8
langchain-core==0.3.21
langchain-text-splitters==0.3.2
langgraph==0.2.55

# --- Embeddings / Transformers ---
# torch/sentence-transformers kaldırıldı — provider=novita (API-based), lokal model kullanılmıyor
# Eğer sentence_transformers provider'a geçilirse buraya ekle: torch sentence-transformers

# --- Vector DB clients ---
qdrant-client==1.12.1
faiss-cpu==1.9.0.post1

# --- Hybrid Retrieval ---
rank-bm25>=0.2.2  # In-context BM25 scoring for exact keyword/path matching (enhanced RAG)

# --- YAML parsing (Rule Engine) ---
PyYAML>=6.0

# --- Cache ---
redis>=5.0.0  # Embedding cache (embedding_cache.py)

# --- PDF read (tekini seç: PyMuPDF yeter) ---
PyMuPDF==1.24.14
pymupdf4llm
pytest==8.3.3
pytest-asyncio==0.24.0
rich==13.9.4

# opsiyonel görselleştirme
matplotlib==3.9.2

# opsiyonel excel işleri
openpyxl==3.1.5

# Prometheus + OpenTelemetry Monitoring
prometheus-client==0.24.1
prometheus-fastapi-instrumentator==7.1.0
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp-proto-grpc==1.40.0
opentelemetry-instrumentation-fastapi==0.61b0
opentelemetry-instrumentation-httpx==0.61b0
# Auth (login/JWT + parola hash) — Docker imajı bunu kuruyor, eksikti
PyJWT>=2.8.0
bcrypt>=4.1.0
