# ============================================================================
# AI-Powered OS Hardening - Production Dependencies (Python 3.12+)
# ============================================================================
#
# Docker ortamı için: requirements-python311.txt (pinned versions, Python 3.11)
# Manuel kurulum için: bu dosya (minimum versions, Python 3.12+)
#
# Installation:
#   pip install -r requirements.txt
#
# Python Version: 3.12+ recommended
# Last Updated: 2026-05-29
# ============================================================================

# --- Core API ---
fastapi>=0.127.0
uvicorn[standard]>=0.38.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.2.2

# --- HTTP / Utils ---
requests>=2.33.0  # Dependabot: <2.33.0 güvenlik açığı
aiohttp>=3.14.0   # transitive — CVE-2026-34993 / CVE-2026-47265 (pip-audit)
httpx>=0.27.0
orjson>=3.11.6
tenacity>=8.5.0

# --- Data / Parsing ---
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
beautifulsoup4>=4.12.0

# --- LLM / RAG ---
groq>=0.14.0
openai>=1.55.0
tiktoken>=0.8.0
cohere>=5.20.0
# NOT: langchain/langchain-community/langchain-core/langchain-text-splitters/langgraph
# KALDIRILDI — proje kodunda sıfır import (grep doğrulandı). Ölü bağımlılıklardı ve
# Dependabot'taki 1 kritik + ~10 high + birkaç medium açığın TEK kaynağıydı (deserialization
# injection, path traversal, XXE, msgpack vb.). RAG kendi splitter'ını kullanır, langchain'e
# bağımlı değil. Tekrar ihtiyaç olursa son güvenli sürümle geri eklenir.

# --- Embeddings ---
# sentence-transformers: lokal model kullanılmıyor, provider=novita (API-based)
# Novita qwen3-embedding-8b API üzerinden embed edildiği için torch gerekmez.
# Eğer sentence_transformers provider'a geçilirse ekle: torch sentence-transformers

# --- Vector DB clients ---
qdrant-client>=1.12.0
faiss-cpu>=1.14.2

# --- Hybrid Retrieval ---
rank-bm25>=0.2.2

# --- YAML parsing (Rule Engine) ---
PyYAML>=6.0

# --- Cache / Session ---
redis>=5.0.0  # Embedding cache + Redis session store + JWT blacklist

# --- Auth / Security ---
PyJWT>=2.8.0  # JWT access token encode/decode (HS256)
bcrypt>=4.1.0  # Password hashing (login)

# --- PDF parsing ---
PyMuPDF>=1.24.0
pymupdf4llm>=0.0.17

# --- ML ---
scikit-learn>=1.5.0
joblib>=1.3.0

# --- Prometheus + OpenTelemetry Monitoring ---
prometheus-client>=0.24.0
prometheus-fastapi-instrumentator>=7.1.0
opentelemetry-api>=1.40.0
opentelemetry-sdk>=1.40.0
opentelemetry-exporter-otlp-proto-grpc>=1.40.0
opentelemetry-instrumentation-fastapi>=0.61b0
opentelemetry-instrumentation-httpx>=0.61b0

# --- Testing ---
pytest>=9.0.3  # Dependabot: <9.0.3 güvenlik açığı
pytest-asyncio>=0.23.0

# --- Dev / Visualization (optional) ---
rich>=13.9.0
matplotlib>=3.9.0
openpyxl>=3.1.0
